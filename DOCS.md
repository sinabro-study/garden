# 문서 생성 규칙
## 1. 문체
- 개조식 문어체로 작성 필수
  - 서술형 종결어미(~합니다, ~입니다) 사용 금지
  - 명사형 또는 '~함', '~임', '~할 것'으로 종결
- 불필요한 수식어 배제, 핵심 정보 위주로 간결하게 작성

## 2. 구조
- 표를 최대한 활용하여 정보 정리
  - 비교, 나열, 속성 정의는 표 우선 사용
  - 표 셀에는 핵심만 기재, 부연 설명은 표 하단 개조식 항목으로 분리
- 구분선(`---`)은 사용하지 않음
- Header로 표시한 부분은 상위 문단과 공백(빈 줄)을 추가하여 가독성 향상할 것
- Header 하단에는 공백(빈 줄)을 추가하지 말 것

## 3. 다이어그램
- 기본 도구는 Mermaid `flowchart LR` 사용
  - 상태 전이, 프로세스 흐름 등 순서·전이 관계는 표 대신 다이어그램으로 표현할 것
  - 텍스트 기반으로 git diff 및 GitHub 렌더링 가능하므로 문서 내 다이어그램의 기본으로 사용
- 작성 규칙
  - 방향은 좌→우(`LR`) 기본
  - 정상 흐름은 실선, 예외 흐름은 점선으로 구분
  - 상태 성격별 색상 구분 (진행: 파랑, 완료: 초록, 예외: 주황)
  - 다이어그램 하단에 범례를 개조식으로 첨부
- 예외: 배치가 중요한 복잡한 개념도·아키텍처 그림은 Excalidraw(`(Draw)` 파일)로 작성 후 임베드
- 다중 플랫폼 호환 (Obsidian · GitHub · Confluence)
  - 다이어그램 소스는 Mermaid 단일 관리, Obsidian·GitHub은 원본 그대로 렌더링
  - Confluence 발행 시점에만 mermaid-cli(`mmdc`)로 이미지 변환 후 첨부할 것 (이미지는 발행 산출물로 취급, 소스와 이중 관리 금지)
  - Excalidraw 그림은 플러그인 Auto-export SVG 활성화 후 표준 마크다운 `![](경로.svg)`로 임베드할 것 (`![[...]]` 임베드는 Obsidian 외부에서 렌더링 불가)

## 4. 저장 위치
- 새로 생성한 문서는 `00_Inbox/`에 저장
- 이후 분류는 PARA 구조(01_Projects, 02_Areas, 03_Resources, 04_Archive)에 따름

